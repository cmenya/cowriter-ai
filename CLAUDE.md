# CoWriter AI — 协同写作系统

> **每次对话开始时，先读取 `memory/` 文件夹中的所有文件，再开始工作。**

AI 辅助写作工作流，结合作者碎碎念、风格档案、参考文章，生成接近作者本人声音的正式稿。

---

## 目录结构

```
CoWriter AI/
├── notes/          # 碎碎念：灵感、碎片想法、随手记录（写作起点）
├── articles/       # 参考文章：每篇文章的内容素材来源
├── my-writing/     # 作者过去的真实文章（风格学习样本）
│   └── style-dna.md   # 从真实文章提炼的写作风格档案
├── soul.md         # 作者自我定位档案（身份、风格、受众）
├── drafts/         # AI 生成的各版本初稿
├── feedback/       # 每篇文章的迭代记录与改动说明
├── final/          # 最终定稿
└── .claude/
    └── commands/   # 已安装的自定义 Skill
```

---

## 完整 SOP

### 第一步：准备输入材料

在开始写新文章前，准备以下任意一项或多项：

**碎碎念**（必要时）
- 把灵感、随想、碎片观点放入 `notes/` 目录
- 文件命名：`{日期}_{关键词}.md`，例：`2026-06-01_拖延灵感.md`
- 不需要成文，一句话、几个词都行

**参考文章**（必要时）
- 把找到的参考内容放入 `articles/` 目录
- 命名格式：`{序号}_{主题角度}.md`，例：`01_心理学角度.md`

**告诉我写作目标**
- 文章主题
- 目标读者（可省略，默认读 soul.md）
- 目标篇幅（默认约 2000 字）

---

### 第二步：生成初稿

我会综合以下所有材料生成 v1 初稿：

| 输入来源 | 用途 |
|----------|------|
| `notes/` 中的碎碎念 | 提取作者原始观点和角度 |
| `articles/` 中的参考文章 | 提取内容素材、数据、论点 |
| `my-writing/style-dna.md` | 对标作者的句式、节奏、语言习惯 |
| `soul.md` | 对标作者身份定位和目标受众 |

初稿保存路径：`drafts/v1_{主题}.md`

生成初稿时默认使用 `/de-AI-writing` Skill 确保声音自然。

---

### 第三步：迭代修改

1. 直接在对话中告诉我修改意见，或写入 `feedback/` 目录
2. 每轮修改生成新版本：`v2_...`、`v3_...`，以此类推
3. 常见迭代方向：
   - 扩充字数 / 调整结构
   - 找真实学术引用（WebSearch 查证，不凭记忆）
   - 排版优化（blockquote、列表、分隔线）
   - 标题调整

---

### 第四步：归档

定稿后：
1. 将最终版存入 `final/{主题}_final.md`
2. 将迭代过程记录写入 `feedback/v1_feedback.md`
3. 可选：更新 GitHub 仓库（`git add` → `git commit` → `git push`）

---

## 命名约定

| 目录 | 格式 | 示例 |
|------|------|------|
| `notes/` | `{日期}_{关键词}.md` | `2026-06-01_拖延灵感.md` |
| `articles/` | `{序号}_{主题角度}.md` | `01_心理学角度.md` |
| `drafts/` | `v{版本}_{主题}.md` | `v1_AI写作趋势.md` |
| `feedback/` | `v1_feedback.md`（每篇一个） | `v1_feedback.md` |
| `final/` | `{主题}_final.md` | `AI写作趋势_final.md` |

---

## 已安装的 Skill

| Skill | 用途 | 触发方式 |
|-------|------|----------|
| `de-AI-writing` | 去 AI 味改写、润色、翻译、审阅 | `/de-AI-writing` |
| `humanizer-zh` | 中文人性化改写 | `/humanizer-zh` |

从 GitHub 安装新 Skill：直接把链接发给我，我来下载并安装到 `.claude/commands/`。

---

## 写作质量检查清单

每次交付前确认：

- [ ] 从作者个人经历切入，不从定义或结论开始
- [ ] 破折号全文 ≤ 2 个
- [ ] 无宏大叙事词（赋能 / 赛道 / 底层逻辑）
- [ ] 学术引用已通过 WebSearch 查证，有研究者真实姓名
- [ ] 关键术语首次出现时加粗
- [ ] 数据 / 研究结论用 blockquote 展示
- [ ] 操作步骤用列表，不堆在段落里
- [ ] 标题风格：疑问式（"为什么……"）优先
- [ ] 结尾有"写在最后"总结段，不用悬空一句话收尾
